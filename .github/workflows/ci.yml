name: CI
on:
  push:
    branches: [main, development]
  pull_request:
jobs:
  ci:
    uses: jubasjl76-eng/smart-pet-ci/.github/workflows/node-ci.yml@main
    with:
      typecheck: false            # `npm run build` runs tsc
      test-command: "npm run build && npm test"
